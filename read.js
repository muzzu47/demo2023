let arr = [2,3,4,5,3];

let newArr = arr.slice(2,0);

remDup();

function remDup(inArr){
    // let output = [];

    // for(i=0;i<inArr.length;i++){
    //     if(inArr.indexOf(inArr[i]) == inArr.lastIndexOf(inArr[i])){
    //         output.push(inArr[i]);
    //     }
    // }

    return 2;
}

newFunc();

const newFunc = (x) => {

}


console.log(remDup(arr));
